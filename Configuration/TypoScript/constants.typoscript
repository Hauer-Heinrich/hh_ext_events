plugin.tx_hhextevents {
    view {
        # cat=plugin.tx_hhextevents/file; type=string; label=Path to template root (FE)
        templateRootPath =
        # cat=plugin.tx_hhextevents/file; type=string; label=Path to template partials (FE)
        partialRootPath =
        # cat=plugin.tx_hhextevents/file; type=string; label=Path to template layouts (FE)
        layoutRootPath =
    }

    persistence {
        # cat=plugin.tx_hhextevents/persistence/010; type=int; label=Default storage PID
        storagePid = 0

        # cat=plugin.tx_hhextevents/persistence/020; type=int; label=Default storage PID of organizations (tt_address)
        storagePidOrganizations = 0

        # cat=plugin.tx_hhextevents/persistence/030; type=int; label=Default storage PID of contact points (tt_address)
        storagePidContactPointAddresses = 0
    }
}
